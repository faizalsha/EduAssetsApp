<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.krishbhatia.eduassets">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.CourseSubjectsActivity"></activity>
        <activity android:name=".SubjectsActivity" />
        <activity android:name=".ui.activities.PurchasedCourseActivity" />
        <activity
            android:name=".ui.activities.CartActivity"
            android:exported="true" />
        <activity android:name=".ui.activities.VideoPlayerActivity" />
        <activity android:name=".ui.activities.PdfViewerActivity" />
        <activity android:name=".ui.activities.ProfileActivity" />
        <activity android:name=".ui.activities.DetailsActivity" />
        <activity
            android:name=".ui.activities.HomePageActivity"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="tutorialwing.com.glidelibrarytutorial.GlideConfiguration"
            android:value="GlideModule" />

        <activity android:name=".ui.activities.RegisterActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.SubjectResourceActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>